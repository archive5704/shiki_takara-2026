const MONTHS=["1月","2月","3月","4月","5月","6月","7月","8月","9月","10月","11月","12月"];
const COLORS=[
  {value:"rose",label:"さくら",color:"#f3cfd9",glyph:"#c06f8a"},
  {value:"peach",label:"もも",color:"#f7d9c7",glyph:"#c77f5d"},
  {value:"lemon",label:"たんぽぽ",color:"#f6ebbe",glyph:"#a5891f"},
  {value:"mint",label:"みどり",color:"#cfead7",glyph:"#4f8a63"},
  {value:"sky",label:"そら",color:"#cfe4f6",glyph:"#4d7ea8"},
  {value:"lavender",label:"ふじ",color:"#ddd7f6",glyph:"#7460b6"},
  {value:"sand",label:"きなり",color:"#eadfcd",glyph:"#8d7653"}
];
const $=(id)=>document.getElementById(id);
const els={yearSelect:$("yearSelect"),prevBtn:$("prevBtn"),nextBtn:$("nextBtn"),addBtn:$("addBtn"),monthTitle:$("monthTitle"),monthHeadline:$("monthHeadline"),filterTrigger:$("filterTrigger"),filterValue:$("filterValue"),filterMenu:$("filterMenu"),sortSelect:$("sortSelect"),manageCatsBtn:$("manageCatsBtn"),viewerNote:$("viewerNote"),wrap:$("wrap"),nameGrid:$("nameGrid"),roadmap:$("roadmap"),legend:$("legend"),editor:$("editor"),editorTitle:$("editorTitle"),toggleEditor:$("toggleEditor"),resetBtn:$("resetBtn"),saveBtn:$("saveBtn"),colorValue:$("colorValue"),colorMenu:$("colorMenu"),categoryValue:$("categoryValue"),categoryMenu:$("categoryMenu"),formMsg:$("formMsg"),detailModal:$("detailModal"),detailTitle:$("detailTitle"),detailMeta:$("detailMeta"),detailPhoto:$("detailPhoto"),detailNote:$("detailNote"),detailClose:$("detailClose"),detailEdit:$("detailEdit"),detailDelete:$("detailDelete"),catModal:$("catModal"),catClose:$("catClose"),catList:$("catList"),catAddInput:$("catAddInput"),catSortInput:$("catSortInput"),catAddBtn:$("catAddBtn"),confirmModal:$("confirmModal"),confirmCancel:$("confirmCancel"),confirmDelete:$("confirmDelete"),catActionModal:$("catActionModal"),catActionTitle:$("catActionTitle"),catActionBody:$("catActionBody"),catActionCancel:$("catActionCancel"),catActionSubmit:$("catActionSubmit"),statusText:$("statusText"),adminLoginBtn:$("adminLoginBtn"),adminLogoutBtn:$("adminLogoutBtn"),loginModal:$("loginModal"),loginEmail:$("loginEmail"),loginPassword:$("loginPassword"),loginSubmit:$("loginSubmit"),loginCancel:$("loginCancel"),loginMsg:$("loginMsg"),imageFile:$("imageFile"),imagePreview:$("imagePreview"),removeImageBtn:$("removeImageBtn"),imageStatus:$("imageStatus")};
const form={name:$("name"),start:$("start"),end:$("end"),categoryId:$("categoryId"),color:$("color"),detail:$("detail")};
let items=[],categories=[],currentYear=new Date().getFullYear(),currentMonth=new Date().getMonth()+1,activeCategory="all",activeSort="start",editingId=null,pendingDelete=null,pendingCategoryAction=null,currentImageUrl="",selectedImageFile=null,removeImageFlag=false;

function makeId(){return(typeof crypto!=="undefined"&&crypto.randomUUID)?crypto.randomUUID():"id_"+Date.now().toString(36)+"_"+Math.random().toString(36).slice(2,12)}
function esc(t){return String(t??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;")}
function escNl(t){return esc(t).replace(/\n/g,"<br>")}
function fmt(v){if(!v)return"";const d=new Date(v+"T00:00:00");return Number.isNaN(d.getTime())?v:(d.getMonth()+1)+"/"+d.getDate()}
function fmtFull(v){if(!v)return"";const d=new Date(v+"T00:00:00");return Number.isNaN(d.getTime())?v:d.getFullYear()+"/"+(d.getMonth()+1)+"/"+d.getDate()}
function daysInMonth(y,m){return new Date(y,m,0).getDate()}
function diffDays(a,b){return Math.floor((b.getTime()-a.getTime())/86400000)}
function colorHex(v){return(COLORS.find(c=>c.value===v)||COLORS[0]).color}
function glyphColor(v){return(COLORS.find(c=>c.value===v)||COLORS[0]).glyph}
function categoryNameById(id){const cat=categories.find(c=>c.id===id);return cat?cat.name:""}
function mountainGlyph(item,d){if(categoryNameById(item.category_id)!=="山菜")return"";const s=new Date(item.start+"T00:00:00"),n=diffDays(s,d);return n<0?"":n<7?"◎":n<14?"○":"△"}
function visible(item,y,m){const ms=new Date(y,m-1,1),me=new Date(y,m-1,daysInMonth(y,m)),s=new Date(item.start+"T00:00:00"),e=new Date(item.end+"T00:00:00");if(Number.isNaN(s)||Number.isNaN(e)||e<ms||s>me)return null;return{start:s<ms?1:s.getDate(),end:e>me?me.getDate():e.getDate()}}
function monthItems(y,m,categoryFilter,sortKey){const ms=new Date(y,m-1,1),me=new Date(y,m-1,daysInMonth(y,m));const arr=items.filter(i=>{if(!i.start||!i.end)return false;if(categoryFilter!=="all"&&i.category_id!==categoryFilter)return false;const s=new Date(i.start+"T00:00:00"),e=new Date(i.end+"T00:00:00");return!(Number.isNaN(s)||Number.isNaN(e)||e<ms||s>me)});arr.sort((a,b)=>{if(sortKey==="category"){return(categoryNameById(a.category_id)+"_"+a.start).localeCompare(categoryNameById(b.category_id)+"_"+b.start,"ja")}if(sortKey==="name"){return String(a.name).localeCompare(String(b.name),"ja")}return String(a.start).localeCompare(String(b.start),"ja")});return arr}
function setPickerValue(el,label,color){el.innerHTML=color?'<span class="swatch" style="background:'+color+'"></span><span>'+esc(label)+'</span>':'<span>'+esc(label)+'</span>'}
function updateColorSelect(){const s=COLORS.find(c=>c.value===form.color.value)||COLORS[0];setPickerValue(els.colorValue,s.label,s.color);els.colorMenu.querySelectorAll(".picker-option").forEach(b=>b.classList.toggle("active",b.dataset.value===form.color.value))}
function updateCategorySelect(){const id=form.categoryId.value;setPickerValue(els.categoryValue,categoryNameById(id)||"カテゴリを選択",null);els.categoryMenu.querySelectorAll(".picker-option").forEach(b=>b.classList.toggle("active",b.dataset.value===id))}
function updateFilterSelect(){const id=activeCategory;setPickerValue(els.filterValue,id==="all"?"すべて":(categoryNameById(id)||"すべて"),null);els.filterMenu.querySelectorAll(".picker-option").forEach(b=>b.classList.toggle("active",b.dataset.value===id))}
function renderCategoryMenus(){els.categoryMenu.innerHTML="";categories.forEach(c=>{const b=document.createElement("button");b.type="button";b.className="picker-option";b.dataset.value=c.id;b.textContent=c.name;els.categoryMenu.appendChild(b)});els.filterMenu.innerHTML="";const all=document.createElement("button");all.type="button";all.className="picker-option";all.dataset.value="all";all.textContent="すべて";els.filterMenu.appendChild(all);categories.forEach(c=>{const b=document.createElement("button");b.type="button";b.className="picker-option";b.dataset.value=c.id;b.textContent=c.name;els.filterMenu.appendChild(b)});updateCategorySelect();updateFilterSelect()}
function populateYears(){els.yearSelect.innerHTML="";const years=new Set([currentYear,currentYear-1,currentYear+1]);items.forEach(i=>{const ys=Number(String(i.start||"").slice(0,4)),ye=Number(String(i.end||"").slice(0,4));if(Number.isFinite(ys))years.add(ys);if(Number.isFinite(ye))years.add(ye)});Array.from(years).sort((a,b)=>a-b).forEach(y=>{const o=document.createElement("option");o.value=String(y);o.textContent=String(y);if(y===currentYear)o.selected=true;els.yearSelect.appendChild(o)})}
function populateColorMenu(){els.colorMenu.innerHTML="";COLORS.forEach(c=>{const b=document.createElement("button");b.type="button";b.className="picker-option";b.dataset.value=c.value;b.innerHTML='<span class="swatch" style="background:'+c.color+'"></span><span>'+c.label+'</span>';els.colorMenu.appendChild(b)});updateColorSelect()}
function populateAll(){populateYears();renderCategoryMenus();populateColorMenu();els.sortSelect.value=activeSort}
function renderLegend(show){els.legend.innerHTML=show?'<span><span>◎</span>最初の7日</span><span><span>○</span>次の7日</span><span><span>△</span>それ以降</span>':""}
function renderRoadmap(list){const d=daysInMonth(currentYear,currentMonth),today=new Date(),todayDay=(today.getFullYear()===currentYear&&today.getMonth()+1===currentMonth)?today.getDate():0;els.roadmap.style.setProperty("--days",String(d));els.roadmap.innerHTML="";els.nameGrid.innerHTML="";const nameHead=document.createElement("div");nameHead.className="name-row head";nameHead.innerHTML='<div class="name">たからもの</div>';els.nameGrid.appendChild(nameHead);const dayHead=document.createElement("div");dayHead.className="day-row head";const dayCells=document.createElement("div");dayCells.className="days";for(let i=1;i<=d;i++){const c=document.createElement("div");c.className="hcell"+(i===todayDay?" today":"");c.textContent=String(i);dayCells.appendChild(c)}dayHead.appendChild(dayCells);els.roadmap.appendChild(dayHead);if(!list.length){const empty=document.createElement("div");empty.className="empty";empty.innerHTML="まだ記録がありません。";els.roadmap.appendChild(empty);renderLegend(false);return}renderLegend(list.some(i=>categoryNameById(i.category_id)==="山菜"));list.forEach(item=>{const nr=document.createElement("div");nr.className="name-row";const n=document.createElement("div");n.className="name clickable";n.innerHTML='<div class="item"><strong>'+esc(item.name)+'</strong><small>'+esc(categoryNameById(item.category_id))+' / '+esc(fmt(item.start))+'〜'+esc(fmt(item.end))+'</small></div>';n.onclick=()=>openDetail(item);nr.appendChild(n);els.nameGrid.appendChild(nr);const dr=document.createElement("div");dr.className="day-row";const days=document.createElement("div");days.className="days";const range=visible(item,currentYear,currentMonth);for(let i=1;i<=d;i++){const c=document.createElement("div");c.className="cell"+(i===todayDay?" today":"");if(range&&i>=range.start&&i<=range.end){const m=document.createElement("div");if(categoryNameById(item.category_id)==="山菜"){m.className="mark mountain";m.style.color=glyphColor(item.color);m.textContent=mountainGlyph(item,new Date(currentYear,currentMonth-1,i))}else{m.className="mark";m.style.background=colorHex(item.color)}c.appendChild(m)}days.appendChild(c)}dr.appendChild(days);els.roadmap.appendChild(dr)})}
function render(){els.monthTitle.textContent=MONTHS[currentMonth-1];els.monthHeadline.textContent=currentYear+"年"+MONTHS[currentMonth-1];renderRoadmap(monthItems(currentYear,currentMonth,activeCategory,activeSort))}
function showMsg(t){els.formMsg.textContent=t;els.formMsg.classList.add("show")}
function clearMsg(){els.formMsg.textContent="";els.formMsg.classList.remove("show")}
function showLoginMsg(t){els.loginMsg.textContent=t;els.loginMsg.classList.add("show")}
function clearLoginMsg(){els.loginMsg.textContent="";els.loginMsg.classList.remove("show")}
function setPreview(url){if(url)els.imagePreview.innerHTML='<img src="'+url+'" alt="preview" />';else els.imagePreview.textContent="画像なし"}
function setImageStatus(t){els.imageStatus.textContent=t||""}
function resetImageState(){currentImageUrl="";selectedImageFile=null;removeImageFlag=false;if(els.imageFile)els.imageFile.value="";setPreview("");setImageStatus("管理用のみ画像アップできます。")}
function startNewRecord(){editingId=null;els.editorTitle.textContent="新しく記録する";els.saveBtn.textContent="保存する";clearMsg();form.name.value="";form.start.value="";form.end.value="";form.categoryId.value=categories[0]?.id||"";form.color.value=COLORS[0].value;form.detail.value="";updateCategorySelect();updateColorSelect();resetImageState()}
function fillForm(item){editingId=item.id;els.editorTitle.textContent="記録を修正する";els.saveBtn.textContent="修正を保存";clearMsg();form.name.value=item.name||"";form.start.value=item.start||"";form.end.value=item.end||"";form.categoryId.value=item.category_id||"";form.color.value=item.color||COLORS[0].value;form.detail.value=item.detail||"";updateCategorySelect();updateColorSelect();currentImageUrl=item.image_url||"";selectedImageFile=null;removeImageFlag=false;if(els.imageFile)els.imageFile.value="";setPreview(currentImageUrl);setImageStatus(currentImageUrl?"現在の画像を使用中":"画像なし");if(els.editor.classList.contains("collapsed"))toggleEditor(true)}
async function reloadAll(){els.statusText.textContent="読み込み中…";try{const data=await Promise.all([window.IyasakaSupabase.fetchCategories(),window.IyasakaSupabase.fetchTreasures()]);categories=data[0].map(c=>({id:c.id,name:c.name||"",sort_order:Number(c.sort_order||0)}));items=data[1].map(x=>({id:x.id,name:x.name||"",start:x.start_date||"",end:x.end_date||"",category_id:x.category_id||"",color:x.color||"rose",detail:x.detail||"",image_url:x.image_url||""}));populateAll();render();els.statusText.textContent=items.length+"件を表示中"}catch(err){els.statusText.textContent=err.message||"読み込みに失敗しました。"}}
async function save(){clearMsg();if(!window.IyasakaAuth.isAdmin())return showMsg("管理者ログインが必要です。");const isEdit=!!editingId;const payload={id:isEdit?editingId:makeId(),name:form.name.value.trim(),start:form.start.value,end:form.end.value,category_id:(form.categoryId.value||"").trim(),color:form.color.value,detail:form.detail.value.trim(),image_url:currentImageUrl||""};if(!payload.name)return showMsg("たからもの名が未入力の為、保存できません。");if(!payload.start||!payload.end)return showMsg("開始日または終了日が未入力の為、保存できません。");if(!payload.category_id)return showMsg("カテゴリが未設定の為、保存できません。");if(payload.end<payload.start)return showMsg("開始日より、終了日が前の為、保存できません。");try{if(removeImageFlag)payload.image_url="";if(selectedImageFile){setImageStatus("画像をアップロード中…");const uploadPromise=window.IyasakaSupabase.uploadImage(selectedImageFile,payload.id);const timeoutPromise=new Promise((_,reject)=>setTimeout(()=>reject(new Error("画像アップロードがタイムアウトしました。Storage設定または通信状況をご確認ください。")),20000));payload.image_url=await Promise.race([uploadPromise,timeoutPromise]);setImageStatus("画像をアップロードしました。")}const row={id:payload.id,name:payload.name,start_date:payload.start,end_date:payload.end,category_id:payload.category_id,color:payload.color,detail:payload.detail,image_url:payload.image_url||""};if(isEdit){await window.IyasakaSupabase.updateTreasure(editingId,{name:row.name,start_date:row.start_date,end_date:row.end_date,category_id:row.category_id,color:row.color,detail:row.detail,image_url:row.image_url})}else{await window.IyasakaSupabase.insertTreasure(row)}currentYear=Number(payload.start.slice(0,4));currentMonth=Number(payload.start.slice(5,7));await reloadAll();startNewRecord()}catch(err){setImageStatus(err.message||"画像アップロードに失敗しました。");showMsg(err.message||"保存に失敗しました。")}}
function openDetail(item){const ms=new Date(currentYear,currentMonth-1,1),me=new Date(currentYear,currentMonth-1,daysInMonth(currentYear,currentMonth)),s=new Date(item.start+"T00:00:00"),e=new Date(item.end+"T00:00:00");const chips=['<span class="chip">カテゴリ '+esc(categoryNameById(item.category_id))+'</span>','<span class="chip">'+esc(fmtFull(item.start))+'〜'+esc(fmtFull(item.end))+'</span>'];if(s<ms)chips.push('<span class="chip">前月から続いています</span>');if(e>me)chips.push('<span class="chip">次月にも続きます</span>');els.detailTitle.textContent=item.name||"";els.detailMeta.innerHTML=chips.join("");els.detailNote.innerHTML=item.detail?escNl(item.detail):"メモなし";if(item.image_url)els.detailPhoto.innerHTML='<img src="'+item.image_url+'" alt="detail" />';else els.detailPhoto.textContent="画像なし";els.detailEdit.onclick=()=>{closeDetail();fillForm(item)};els.detailDelete.onclick=()=>{pendingDelete=item;els.confirmModal.classList.add("open")};els.detailModal.classList.add("open")}
function closeDetail(){els.detailModal.classList.remove("open")}
function closeConfirm(){els.confirmModal.classList.remove("open");pendingDelete=null}
async function deletePending(){if(!pendingDelete)return;try{await window.IyasakaSupabase.deleteTreasure(pendingDelete.id);closeConfirm();closeDetail();await reloadAll()}catch(err){alert(err.message||"削除に失敗しました。")}}
function openCatModal(){renderCategoryManager();els.catModal.classList.add("open")}
function closeCatModal(){els.catModal.classList.remove("open")}
function renderCategoryManager(){els.catList.innerHTML="";if(!categories.length){els.catList.innerHTML='<div class="small-note">カテゴリがありません</div>';return}categories.forEach(cat=>{const row=document.createElement("div");row.className="cat-row";const name=document.createElement("div");name.className="cat-name";name.innerHTML='<strong>'+esc(cat.name)+'</strong><div class="cat-sort">並び順: '+esc(String(cat.sort_order??0))+'</div>';const renameBtn=document.createElement("button");renameBtn.type="button";renameBtn.className="btn";renameBtn.textContent="名称変更";renameBtn.onclick=()=>openRenameCategoryModal(cat);const deleteBtn=document.createElement("button");deleteBtn.type="button";deleteBtn.className="btn btn-danger";deleteBtn.textContent="削除";deleteBtn.onclick=()=>openDeleteCategoryModal(cat);row.append(name,renameBtn,deleteBtn);els.catList.appendChild(row)})}
async function addCategory(){const name=els.catAddInput.value.trim();const sortOrder=Number(els.catSortInput.value||0);if(!name)return;if(categories.some(c=>c.name===name))return alert("同じ名前のカテゴリが既にあります");try{await window.IyasakaSupabase.insertCategory(name,sortOrder);els.catAddInput.value="";els.catSortInput.value="";await reloadAll();renderCategoryManager()}catch(err){alert(err.message||"カテゴリ追加に失敗しました。")}}
function closeCatActionModal(){els.catActionModal.classList.remove("open");pendingCategoryAction=null}
function openRenameCategoryModal(cat){pendingCategoryAction={type:"rename",cat};els.catActionTitle.textContent="カテゴリ名変更";els.catActionBody.innerHTML='<div class="field"><label for="catRenameInput">新しいカテゴリ名</label><input id="catRenameInput" type="text" value="'+esc(cat.name)+'" /></div><div class="field"><label for="catRenameSort">並び順</label><input id="catRenameSort" type="number" value="'+esc(String(cat.sort_order??0))+'" /></div>';els.catActionSubmit.textContent="変更する";els.catActionSubmit.className="btn btn-primary";els.catActionModal.classList.add("open")}
function openDeleteCategoryModal(cat){pendingCategoryAction={type:"delete",cat};const options=categories.filter(c=>c.id!==cat.id).map(c=>'<option value="'+esc(c.id)+'">'+esc(c.name)+'</option>').join("");els.catActionTitle.textContent="カテゴリ削除";els.catActionBody.innerHTML='<div class="field"><label>削除するカテゴリ</label><input type="text" value="'+esc(cat.name)+'" disabled /></div><div class="field"><label for="catMoveSelectInline">記録の移動先</label><select id="catMoveSelectInline" class="select">'+options+'</select></div>';els.catActionSubmit.textContent="削除する";els.catActionSubmit.className="btn btn-danger";els.catActionModal.classList.add("open")}
async function submitCategoryAction(){if(!pendingCategoryAction)return;try{if(pendingCategoryAction.type==="rename"){const nextName=$("catRenameInput").value.trim();const nextSort=Number($("catRenameSort").value||0);if(!nextName)return alert("カテゴリ名を入力してください");await window.IyasakaSupabase.updateCategory(pendingCategoryAction.cat.id,{name:nextName,sort_order:nextSort})}else{const moveTo=$("catMoveSelectInline").value;if(!moveTo)return alert("移動先カテゴリを選んでください");await window.IyasakaSupabase.moveTreasuresToCategory(pendingCategoryAction.cat.id,moveTo);await window.IyasakaSupabase.deleteCategory(pendingCategoryAction.cat.id)}closeCatActionModal();await reloadAll();renderCategoryManager()}catch(err){alert(err.message||"カテゴリ操作に失敗しました。")}}
function toggleEditor(force){const open=force===undefined?els.editor.classList.contains("collapsed"):force;els.editor.classList.toggle("collapsed",!open);els.toggleEditor.textContent=open?"折りたたむ":"開く"}
function openLoginModal(){clearLoginMsg();els.loginEmail.value="";els.loginPassword.value="";els.loginModal.classList.add("open")}
function closeLoginModal(){els.loginModal.classList.remove("open")}
function clearLoginMsg(){els.loginMsg.textContent="";els.loginMsg.classList.remove("show")}
function showLoginMsg(t){els.loginMsg.textContent=t;els.loginMsg.classList.add("show")}
function applyAdminState(){const viewer=!window.IyasakaAuth.isAdmin();document.querySelectorAll(".admin-only").forEach(el=>el.classList.toggle("hidden",viewer));els.editor.classList.toggle("hidden",viewer);els.adminLoginBtn.classList.toggle("hidden",!viewer);els.adminLogoutBtn.classList.toggle("hidden",viewer);els.viewerNote.textContent=viewer?"閲覧用：公開データを見る画面です。":"管理用：Supabaseに保存できます。";if(viewer){closeDetail();closeConfirm();closeCatModal();closeCatActionModal()}}
els.prevBtn.addEventListener("click",()=>{if(currentMonth===1){currentMonth=12;currentYear-=1}else currentMonth-=1;populateYears();render()});
els.nextBtn.addEventListener("click",()=>{if(currentMonth===12){currentMonth=1;currentYear+=1}else currentMonth+=1;populateYears();render()});
els.yearSelect.addEventListener("change",()=>{currentYear=Number(els.yearSelect.value);render()});
els.sortSelect.addEventListener("change",()=>{activeSort=els.sortSelect.value;render()});
els.addBtn.addEventListener("click",()=>{startNewRecord();const sm=String(currentMonth).padStart(2,"0");form.start.value=currentYear+"-"+sm+"-01";form.end.value=currentYear+"-"+sm+"-01";if(els.editor.classList.contains("collapsed"))toggleEditor(true)});
els.toggleEditor.addEventListener("click",()=>toggleEditor());
els.saveBtn.addEventListener("click",save);
els.resetBtn.addEventListener("click",startNewRecord);
els.filterTrigger.addEventListener("click",()=>els.filterMenu.classList.toggle("open"));
$("categoryTrigger").addEventListener("click",()=>$("categoryMenu").classList.toggle("open"));
$("colorTrigger").addEventListener("click",()=>$("colorMenu").classList.toggle("open"));
els.filterMenu.addEventListener("click",(e)=>{const b=e.target.closest(".picker-option");if(!b)return;activeCategory=b.dataset.value;updateFilterSelect();els.filterMenu.classList.remove("open");render()});
els.categoryMenu.addEventListener("click",(e)=>{const b=e.target.closest(".picker-option");if(!b)return;form.categoryId.value=b.dataset.value;updateCategorySelect();els.categoryMenu.classList.remove("open")});
els.colorMenu.addEventListener("click",(e)=>{const b=e.target.closest(".picker-option");if(!b)return;form.color.value=b.dataset.value;updateColorSelect();els.colorMenu.classList.remove("open")});
document.addEventListener("click",(e)=>{if(!e.target.closest("#filterTrigger")&&!e.target.closest("#filterMenu"))els.filterMenu.classList.remove("open");if(!e.target.closest("#categoryTrigger")&&!e.target.closest("#categoryMenu"))els.categoryMenu.classList.remove("open");if(!e.target.closest("#colorTrigger")&&!e.target.closest("#colorMenu"))els.colorMenu.classList.remove("open")});
els.detailClose.addEventListener("click",closeDetail);
els.detailModal.addEventListener("click",(e)=>{if(e.target===els.detailModal)closeDetail()});
els.confirmCancel.addEventListener("click",closeConfirm);
els.confirmDelete.addEventListener("click",deletePending);
els.confirmModal.addEventListener("click",(e)=>{if(e.target===els.confirmModal)closeConfirm()});
els.manageCatsBtn.addEventListener("click",openCatModal);
els.catClose.addEventListener("click",closeCatModal);
els.catModal.addEventListener("click",(e)=>{if(e.target===els.catModal)closeCatModal()});
els.catAddBtn.addEventListener("click",addCategory);
els.catActionCancel.addEventListener("click",closeCatActionModal);
els.catActionSubmit.addEventListener("click",submitCategoryAction);
els.catActionModal.addEventListener("click",(e)=>{if(e.target===els.catActionModal)closeCatActionModal()});
els.wrap.addEventListener("scroll",()=>{els.nameGrid.scrollTop=els.wrap.scrollTop});
els.adminLoginBtn.addEventListener("click",openLoginModal);
els.adminLogoutBtn.addEventListener("click",async()=>{await window.IyasakaAuth.signOut();applyAdminState()});
els.loginCancel.addEventListener("click",closeLoginModal);
els.loginModal.addEventListener("click",(e)=>{if(e.target===els.loginModal)closeLoginModal()});
els.loginSubmit.addEventListener("click",async()=>{clearLoginMsg();try{await window.IyasakaAuth.signIn(els.loginEmail.value.trim(),els.loginPassword.value);closeLoginModal();applyAdminState()}catch(err){showLoginMsg(err.message||"ログインに失敗しました。")}});
if(els.imageFile){els.imageFile.addEventListener("change",(e)=>{const file=e.target.files&&e.target.files[0];if(!file){selectedImageFile=null;return}selectedImageFile=file;removeImageFlag=false;const url=URL.createObjectURL(file);setPreview(url);setImageStatus("新しい画像を選択しました。保存で反映されます。")})}
if(els.removeImageBtn){els.removeImageBtn.addEventListener("click",()=>{selectedImageFile=null;removeImageFlag=true;currentImageUrl="";if(els.imageFile)els.imageFile.value="";setPreview("");setImageStatus("画像を外します。保存で反映されます。")})}
document.addEventListener("keydown",(e)=>{if(e.key==="Escape"){if(els.loginModal.classList.contains("open"))closeLoginModal();else if(els.confirmModal.classList.contains("open"))closeConfirm();else if(els.catActionModal.classList.contains("open"))closeCatActionModal();else if(els.catModal.classList.contains("open"))closeCatModal();else if(els.detailModal.classList.contains("open"))closeDetail()}});
document.addEventListener("iyasaka-auth-changed",()=>applyAdminState());
(async function boot(){await window.IyasakaAuth.init();applyAdminState();populateAll();startNewRecord();toggleEditor(true);await reloadAll()})();
